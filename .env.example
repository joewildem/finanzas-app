# Frontend (safe to expose to the browser — never put the Secret key here)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Server-side only (Supabase CLI, seed/admin scripts) — NEVER prefix with VITE_,
# NEVER import from src/, NEVER commit the real value.
SUPABASE_SECRET_KEY=
